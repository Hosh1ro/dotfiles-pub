[core]
	safecrlf = true
	autocrlf = input

[init]
	defaultBranch = main

[pull]
	ff = only

[color]
	ui = auto

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
